# Skill library registry (Phase 0)

Census of `.agents/skills/` on 2026-09-23. **37** skill folders. `improve-skill-library` is historical — live owner is `align-harness`.

| name | one-sentence outcome ("done") | lifecycle phase | mutates? | scope | inputs (artifacts consumed) | outputs (artifacts produced) | owns-concepts (SSOT it claims) | links-to (skills/docs referenced) |
|------|-------------------------------|-----------------|----------|-------|----------------------------|------------------------------|--------------------------------|----------------------------------|
| router | Picks one primary skill for the situation and invokes it in the same turn. | meta (routing) | read-only | repo-wide | user message; thread state; `src/config/app-tasks.json`; active `DEVELOPMENT_PLAN.md`; `documentation/DOC_APP_VISION.md` | next-skill dispatch (no file) | routing gates 1–3; situation→skill map; app-task backlog intake rules; thread continuation | all `.agents/skills/*`; `router/references/dev-cycle-matrix.md`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `documentation/DOC_APP_VISION.md`; `~/.cursor/skills-cursor/babysit` (ref) |
| prime | Delivers a concise project context report aligned with rules, architecture, and active state. | prime | read-only | repo-wide | `git` state; `ARCHITECTURE.md`; `documentation/DOC_INDEX.md`; `.cursor/rules/INDEX.md`; `package.json`; `documentation/DOC_APP_VISION.md` | chat report (overview, architecture, current state, insights) | session technical grounding procedure | `validate`; `plan`; `start`; `grill-me`; `documentation/DOC_TESTING.md` |
| grill-me | Optional warm-start scope-edge alignment; closed decisions logged to `DECISIONS.md`. | clarify | writes (`DECISIONS.md`) | single-feature | user intent; `documentation/DOC_APP_VISION.md`; neighbor code paths | chat summary; `DECISIONS.md` | pre-plan / gate-1 warm start; shares ledger with `plan-grill` | `feature`; `plan`; `plan-grill`; `quick-piv`; `prime`; `router` § Plan corridor flow |
| plan-grill | Plan-corridor rail: fork checklist each phase; ties asked, clear-winners logged; anti-dup via `DECISIONS.md`. | plan (rail) | writes (`DECISIONS.md`) | single-feature | active `plan` phase; `DECISIONS.md`; fork cues | updated `DECISIONS.md`; locked choice for same phase | continuous plan-grill rail + mandatory fork checklist | `plan`; `grill-me`; `plan-grill-auto`; `pattern-review`; `quick-piv`; `router` § Plan corridor flow; `plan-grill/references/decisions-template.md` |
| plan-grill-auto | Same plan-grill checklist; agent answers every fork, logs Closed rows, skips Present, then implement for user test. | plan (rail) | writes (`DECISIONS.md`) | single-feature | user forbids questions; active `plan` phase; `DECISIONS.md` | Closed `DECISIONS.md` rows (`source: plan-grill-auto`); handoff to `implement` | who answers forks + no Present wait | `plan-grill`; `plan`; `grill-me`; `pattern-review`; `dont-reinvent-the-wheel`; `review-dev-plan`; `implement`; `quick-piv` |
| start | Human onboarding gates passed and verification checklist confirmed by user. | clarify / meta (onboarding) | writes code | repo-wide | `README.md`; `src/config/app-tasks.json`; `documentation/DOC_APP_VISION.md`; user web actions | configured fork; `ACTIVE` vision; updated/archived tasks | human first-time setup procedure; app-vision gate during onboarding | `prime`; `plan`; `feature`; `documentation/DOC_APP_VISION.md`; `.cursor/rules/git-workflow/RULE.mdc` |
| feature | Product/requirements artifact complete through Phase 4 with mandatory 🔴 stops and user approval; product answers logged to `DECISIONS.md`. | clarify → plan | writes (`DECISIONS.md`); hands off execution | single-feature | `documentation/DOC_APP_VISION.md`; `.cursor/rules/*`; codebase search | requirements/spec (chat/doc); `DECISIONS.md`; handoff to `plan` | new-feature product process with decision stops and journey mapping | `plan`; `plan-grill`; `pattern-review`; `implement`; `validate`; `finish`; `grill-me`; `router/references/dev-cycle-matrix.md` |
| plan | `DEVELOPMENT_PLAN.md` written with compliance, complexity, pattern section, and per-phase gates. | plan | writes code | single-feature | user request; rules; `documentation/DOC_APP_VISION.md`; codebase investigation; `DECISIONS.md` | `documentation/jobs/temp_job_*/DEVELOPMENT_PLAN.md` | engineering execution plan format and procedure | `prime`; `validate`; `quick-piv`; `implement`; `review-dev-plan`; `pattern-review`; `plan-grill`; `plan-grill-auto`; `finish`; `plan/references/*`; `router/references/dev-cycle-matrix.md` |
| pattern-review | Industry precedent verdict delivered; Pattern risk alert and plan section when required. Auto mode returns A/B/C without an owner stop. | pattern-review | read-only (optional plan edits) | targeted | plan/proposal; `pattern-review/references/rubric.md` | chat verdict; § Pattern & precedent in plan | industry/product precedent rubric and alert template | `layer-consistency-check`; `standards-align`; `validate`; `plan`; `plan-grill-auto`; `review-dev-plan`; `feature`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md` |
| review-dev-plan | Six-lens plan critique synthesized; Plan review row set Done when the user accepts, or when `plan-grill-auto` records agent-accept. | review-dev-plan | read-only (optional Summary/Decisions edits) | targeted | `DEVELOPMENT_PLAN.md`; `ARCHITECTURE.md`; rules; pattern-review rubric | chat synthesis; `Plan review: Done` in plan | multi-lens qualitative plan critique procedure | `validate`; `pattern-review`; `plan`; `plan-grill-auto`; `feature`; `implement` |
| layer-consistency-check | One-beat layer check done; user chose workaround vs structural path before code proceeds. | check (proactive) | read-only until path chosen; then writes | targeted | request cues; diff; `references/workaround-shapes.md` | Layer consistency verdict; WARNING/WORKAROUND alert | internal layer/workaround detection rubric and alert template | `pattern-review`; `standards-align`; `consolidate`; `validate`; `improve`; `challenge`; `.cursor/rules/architecture/RULE.mdc` |
| implement | All `DEVELOPMENT_PLAN.md` phases executed, gates passed, plan doc updated. | implement | writes code | single-feature | `DEVELOPMENT_PLAN.md`; optional `DECISIONS.md` (soft-warn if open; auto resolves Open); rules; optional `documentation/DOC_APP_VISION.md` | source changes; updated plan (Notes, Decisions, phase status) | phase-by-phase execution against `DEVELOPMENT_PLAN.md` | `plan`; `quick-piv`; `review-dev-plan`; `validate`; `pattern-review`; `prime`; `finish`; `debug`; `plan-grill`; `plan-grill-auto` |
| quick-piv | XS/S scoped change landed with chat or inline quick plan and inline tooling pass. | plan → implement → validate (compressed) | writes code | single-feature / targeted | optional `DEVELOPMENT_PLAN.md` or chat quick plan | source changes; optional plan updates; inline tooling report | compressed Plan→Implement→Validate orchestration for XS/S | `plan`; `plan-grill-auto`; `implement`; `validate`; `finish`; `prime`; `debug`; `router/references/dev-cycle-matrix.md` |
| debug | Uncertainty narrowed via hypotheses and evidence until user declares resolution. | implement (incident) | writes code | targeted | repro; logs; `debug/patterns.md` | debug state reports; fixes; optional pattern updates via `finish` | scientific debugging procedure; `debug/patterns.md` library | `hypothesis`; `learn`; `finish`; `quick-piv`; `implement` |
| hypothesis | Hypothesis space shrinks each iteration via pre-registered PROVED/DISPROVED blocks. | implement (incident) | writes code | targeted | bug context; ruled-out tally | hypothesis blocks; targeted fixes | pre-registered hypothesis debugging loop | `debug`; `router` |
| validate | Validation report synthesized for auto-selected depth; fixes only if user requests. | validate / check | read-only (writes if user requests fixes) | targeted → repo-wide | plan or diff; `.cursor/rules/*`; tooling commands | validation report (chat) | mode auto-selection; subagent JSON schema; tooling pass lists | `pattern-review`; `review-dev-plan`; `review`; `finish`; `push`; `consolidate`; `plan/references/rules-registry.md` |
| review | 170-point component rubric scored with section breakdown and threshold status. | review | read-only | single-component | target component source | scored rubric table in chat | React/MUI component quality rubric (170pt) | `quick-piv`; `implement`; `validate`; `finish` |
| improve | User picks ≤3 plain-language findings; downstream child skill invoked. | check / meta (facade) | read-only (delegates mutation) | targeted (product/feature/component) | user pain; `documentation/DOC_APP_VISION.md`; silent lite audit | IMPROVE block; child-skill handoff | improve facade routing and plain-language labels | `challenge`; `standards-align`; `consolidate`; `layer-consistency-check`; `validate`; `review`; `grill-me`; `router` |
| standards-align | Should-we and how-to industry-align decisions recorded; execution skill invoked. | check | read-only until handoff | product / feature / component | named scope; `pattern-review/references/rubric.md` | STANDARDS ALIGN report; handoff | should-align gate + how orchestration (not rubric SSOT) | `pattern-review`; `improve`; `challenge`; `consolidate`; `layer-consistency-check`; `grill-me`; `plan`; `quick-piv` |
| challenge | User-chosen simplification option (A/B/C/D) implemented with relevant checks. | check | writes code (after gate) | single-feature | target workflow; user goal; constraints | FEATURE CHALLENGE report; code changes | flow+code simplification challenge procedure | `improve`; `standards-align`; `consolidate`; `optimize2`; `quick-piv`; `implement` |
| consolidate | Consolidation audit presented; user-approved extractions executed and verified. | check | writes code (after approval) | repo-wide (default) | `src/` scans; git churn | audit map; shared abstractions; placement fixes | cross-codebase redundancy discovery; `references/semantic-placement.md` | `optimize2`; `challenge`; `standards-align`; `validate`; `layer-consistency-check`; `improve` |
| optimize2 | User-chosen 4-level optimization executed with tests and verification. | check | writes code (after gate) | targeted (hotspot) | hotspot files; git churn; ESLint complexity | HOTSPOT analysis; refactored code | 4-level optimization framework; Rule of Three | `consolidate`; `challenge`; `finish`; `react-perf-vite`; `web-perf` plugin (ref) |
| react-perf-vite | Stack-native perf rules applied for the identified symptom. | check | writes code (when fixing) | targeted | symptom; `react-perf-vite/rules/*.md` | perf fixes per applicable rules | Vite SPA React perf rule index and `rules/` | `documentation/DOC_REACT_PERF.md`; `optimize2`; `web-perf` plugin (ref); `review` |
| finish | Local commit complete with version/changelog when required and user-test handoff emitted. | finish | commits | targeted (session scope) | staged diff; `src/config/app-tasks.json`; `scripts/change-classify.cjs` | git commit; optional version/changelog; archived tasks; handoff card | semver mapping; commit message SSOT; user-test handoff; concurrent-work smoke protocol | `push`; `validate`; `debug/patterns.md`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `.cursor/rules/git-workflow/RULE.mdc`; `.cursor/rules/agent-behavior/RULE.mdc` |
| bundle-ship | Full working-tree snapshot bundled into one `finish` commit then pushed. | finish → push | commits + pushes | repo-wide | full git diff; user bundle approval | single commit; remote push | multi-thread bundle orchestration overrides for `finish` | `finish`; `push`; `validate` |
| push | Already-committed work pushed to remote after safety checks and confirmation. | push | pushes | targeted (branch) | clean tree; commits from `finish` | remote branch update | push-only safety flow (no commit) | `finish`; `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `~/.cursor/skills-cursor/babysit` (ref) |
| api-integrate | Phase 1 contract and Phase 2 wire shape documented for unfamiliar vendor/API. | integrations | read-only | targeted (vendor/API) | vendor name; MCP catalog; official docs | contract report; sample/wire-shape notes | MCP fallback tree; two-phase API research procedure; vendor references | `plan` § Foundation validation; `router`; `.cursor/rules/api-integration/RULE.mdc`; `references/vendor-*.md` |
| write-adoption-guide | Cross-repo adoption guide written and workflow layers doc linked. | meta / documentation | writes code | targeted (topic/pattern) | SSOT skill/rule in source repo; topic slug | `documentation/handoffs/*_ADOPTION_GUIDE.md` | adoption guide template; voice-and-naming; layer-mapping config | `documentation/DOC_AGENT_WORKFLOW_LAYERS.md`; `router`; `write-adoption-guide/references/*` |
| learn | Durable lesson persisted to one primary rule, skill, or doc location. | meta | writes code | repo-wide (guidance) | recent diffs; conversation; CI output | updated `RULE.mdc` / `SKILL.md` / `DOC_*` / `patterns.md` | lesson routing; reverse-audit of misleading guidance | `rule-quality`; `finish`; `debug/patterns.md`; `.cursor/rules/INDEX.md` |
| rule-quality | Attached rule/command graded (A–F) or rewritten per quality standards. | meta / rules | read-only (Mode A); writes (Mode B) | targeted (single rule/command) | attached `RULE.mdc` or `SKILL.md` text | grade report or improved rule text | rule/command quality rubric (6 weighted criteria) | `learn`; `align-harness` |
| align-harness | Harness coherence audit done: issue table gated, approved rows applied, no-loss pass at zero missing. | meta | writes code | repo-wide (harness corpus) | `AGENTS.md`; rules; skills; layers doc; `harness_scan` | issue table under `documentation/jobs/harness/`; edited harness files after approval | harness SSOT; lens briefs; no-loss gate | `router`; `update-harness`; `create-skill`; `DOC_AGENT_WORKFLOW_LAYERS.md`; `documentation/jobs/harness/` |
| update-harness | External harness drop classified, reported, and applied only after confirmation. | meta | writes code | repo-wide (harness) | `.agents/harness-inbox/` | `documentation/jobs/harness/*-UPDATE_REPORT.md`; merged skills/rules | intake disposition | `align-harness`; `create-skill` |
| dont-reinvent-the-wheel | Reuse-vs-custom recommendation recorded before custom generic code. | plan (investigate) | read-only | targeted | planned subsystem; current deps | recommendation in plan Conflict & compliance | package/pattern reuse check | `plan`; `feature`; `pattern-review`; `implement`; `quick-piv` |
| modularity-review | Replaceability report produced (`arch:check` + `modularity:report`) without unwrapping imports. | check | read-only (report) | targeted | `src/features/`; vendor imports | modularity report | feature and stack replaceability scores | `plan`; `implement`; `quick-piv`; `architecture/RULE.mdc` |
| create-skill | New or improved `SKILL.md` with optional evals and optimized description. | meta | writes code | targeted (single skill) | user intent; drafts; eval prompts | `SKILL.md`; `evals/`; benchmark artifacts | skill authoring and eval iteration procedure | `align-harness`; `rule-quality` |
| purge-skill | Named feature fully removed with two-layer reference map verified and build green. | engineering | writes code (after approval) | targeted (named feature/module) | target name; repo; `scripts/reference_scan.py`; `references/asset-checklist.md` | removal plan; deleted assets; final report | systematic feature removal (structural + textual reference map) | `purge-skill/references/asset-checklist.md`; `purge-skill/scripts/reference_scan.py` |
| caveman | Ultra-compressed responses active until user says stop. | meta (communication overlay) | read-only | N/A (style) | any conversation | terse chat prose | caveman communication style rules | none (overlay; not a workflow step) |

**Removed / merged (historical):** `check` → `validate` (gate depth); `stepback` → `grill-me`; `grade-rule`+`improve-rule` → `rule-quality`; `improve-skill-library` → `align-harness`; airtable pair → `airtable-inspect`; `architecture-repair2` → `consolidate` § Semantic placement; `complete-setup` → `start` § Teardown; `airtable-inspect` → `api-integrate`.
